<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.imooc.mapper.OrdersMapperCustom" >
  <resultMap id="myOrdersVO" type="com.imooc.pojo.vo.MyOrdersVO">
    <id column="orderId" property="orderId" />
    <result column="createdTime" property="createdTime" />
    <result column="payMethod" property="payMethod" />
    <result column="realPayAmount" property="realPayAmount" />
    <result column="postAmount" property="postAmount" />
    <result column="orderStatus" property="orderStatus" />

    <collection property="subOrderItemList" ofType="com.imooc.pojo.vo.MySubOrderItemVO">
      <id column="itemId" property="itemId" />
      <result column="itemName" property="itemName" />
      <result column="itemImg" property="itemImg" />
      <result column="itemSpecId" property="itemSpecId" />
      <result column="itemSpecName" property="itemSpecName" />
      <result column="buyCounts" property="buyCounts" />
      <result column="price" property="price" />
    </collection>
  </resultMap>

  <select id="getMyOrders" parameterType="map" resultMap="myOrdersVO">
    SELECT od.id orderId,od.created_time createdTime,od.pay_method payMethod,
    od.real_pay_amount realPayAmount,od.post_amount postAmount,os.order_status orderStatus,
    oi.item_id itemId,oi.item_name itemName,oi.item_img itemImg,oi.item_spec_id itemSpecId,
    oi.item_spec_name itemSpecName,oi.buy_counts buyCounts,oi.price price
    FROM orders od
    LEFT JOIN order_status os
    ON od.id = os.order_id
    LEFT JOIN order_items oi
    ON od.id = oi.order_id
    WHERE od.user_id = #{paramMap.userId}
    AND od.is_delete = 0
    <if test="paramMap.orderStatus != null">
      AND os.order_status = #{paramMap.orderStatus}
    </if>
    ORDER BY od.updated_time ASC
  </select>
</mapper>